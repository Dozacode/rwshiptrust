# Migration `20201208110238-create-dpat`

This migration has been generated by Doza at 12/8/2020, 11:02:38 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Fmea" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "Owner" TEXT NOT NULL,
    "vesselname" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "date" DATETIME NOT NULL,
    "author" TEXT NOT NULL,
    "classed" BOOLEAN NOT NULL,
    "classsociety" TEXT,
    "reviewed" BOOLEAN NOT NULL
)

CREATE TABLE "Dpat" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "Owner" TEXT NOT NULL,
    "vesselname" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "date" TEXT NOT NULL,
    "author" TEXT NOT NULL,
    "findings" BOOLEAN NOT NULL,
    "anyAFindings" BOOLEAN NOT NULL,
    "aFindings" TEXT,
    "anyBFindings" BOOLEAN NOT NULL,
    "bFindings" TEXT,
    "anyCFindings" BOOLEAN NOT NULL,
    "cFindings" TEXT NOT NULL,
    "classed" BOOLEAN NOT NULL,
    "classsociety" TEXT,
    "reviewed" BOOLEAN NOT NULL
)

CREATE TABLE "Particular" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "loa" TEXT NOT NULL,
    "flag" TEXT NOT NULL,
    "hull" TEXT NOT NULL,
    "maxdraft" INTEGER NOT NULL,
    "dpSystem" BOOLEAN NOT NULL,
    "dpMake" TEXT NOT NULL,
    "dieselelectric" BOOLEAN NOT NULL
)

CREATE TABLE "Dp" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "dpclass" TEXT NOT NULL,
    "manufacturer" TEXT NOT NULL,
    "windnumber" INTEGER NOT NULL,
    "wind1" TEXT NOT NULL,
    "wind2" TEXT,
    "wind3" TEXT,
    "gyronumber" INTEGER NOT NULL,
    "gyro1" TEXT NOT NULL,
    "gyro2" TEXT,
    "gyro3" TEXT,
    "gyro4" TEXT,
    "vrunumber" BOOLEAN NOT NULL,
    "vru1" TEXT NOT NULL,
    "vru2" TEXT,
    "vru3" TEXT,
    "vru4" TEXT
)

CREATE UNIQUE INDEX "Fmea.vesselname_unique" ON "Fmea"("vesselname")

CREATE UNIQUE INDEX "Dpat.vesselname_unique" ON "Dpat"("vesselname")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201208110238-create-dpat
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,86 @@
+datasource DS {
+  // optionally set multiple providers
+  // example: provider = ["sqlite", "postgresql"]
+  provider = "sqlite"
+  url = "***"
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = "native"
+}
+
+// Define your own datamodels here and run `yarn redwood db save` to create
+// migrations for them.
+// TODO: Please remove the following example:
+model Fmea {
+  id           Int      @id @default(autoincrement())
+  Owner        String
+  vesselname   String   @unique
+  title        String
+  date         DateTime
+  author       String
+  classed      Boolean
+  classsociety String?
+  reviewed     Boolean
+
+
+}
+
+model Dpat {
+  id           Int     @id @default(autoincrement())
+  Owner        String
+  vesselname   String  @unique
+  title        String
+  date         String
+  author       String
+  findings     Boolean
+  anyAFindings Boolean
+  aFindings    String?
+  anyBFindings Boolean
+  bFindings    String?
+  anyCFindings Boolean
+  cFindings    String
+  classed      Boolean
+  classsociety String?
+  reviewed     Boolean
+
+
+}
+
+model Particular {
+  id             Int     @id @default(autoincrement())
+  loa            String
+  flag           String
+  hull           String
+  maxdraft       Int
+  dpSystem       Boolean
+  dpMake         String
+  dieselelectric Boolean
+
+
+
+
+
+}
+
+model Dp {
+  id           Int     @id @default(autoincrement())
+  dpclass      String
+  manufacturer String
+  windnumber   Int
+  wind1        String
+  wind2        String?
+  wind3        String?
+  gyronumber   Int
+  gyro1        String
+  gyro2        String?
+  gyro3        String?
+  gyro4        String?
+  vrunumber    Boolean
+  vru1         String
+  vru2         String?
+  vru3         String?
+  vru4         String?
+
+}
```


